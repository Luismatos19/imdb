import styled from "styled-components";

import * as color from "../../config/colors";

export const Container = styled.div`
  background-color: ${color.PURPLE_DARKEST};
  height: 20em;
`;

export const Texto = styled.h1`
  font-weight: 700;
  font-size: 3em;
  color: ${color.WHITE};
`;
